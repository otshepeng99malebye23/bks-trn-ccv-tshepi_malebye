apiVersion: v1
kind: Service
metadata:
  name: {{ include "credit-card-chart.fullname" . }}
  labels:
    {{- include "credit-card-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "credit-card-chart.selectorLabels" . | nindent 4 }}
