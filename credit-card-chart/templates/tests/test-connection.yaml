apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "credit-card-chart.fullname" . }}-test-connection"
  labels:
    {{- include "credit-card-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "credit-card-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
